declaration: 
  description: "test get"

task:
  assign:
    f: ${incoming.params}
    x:
      y: 
        - ${incoming.params}
        - ${incoming.headers}

branch:
  switch:
  - condition: ${f.h == 1}
    next: branched

anotherTask:
  return: ${x}
  next: end

branched:
  return: ${f}
 