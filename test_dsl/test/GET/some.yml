init: 
  assign:
    x: ${1}
    repeat: ${incoming.headers.repeat || 10}


inc:
  assign:
    x: ${x + 1}

cond:
  switch:
    - condition: ${x > repeat}
      next: toReturn
  next: inc

toReturn:
  return: x increased to ${x}